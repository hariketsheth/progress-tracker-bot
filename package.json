{
  "name": "technocrats-bot",
  "version": "1.0.0",
  "scripts": {
    "start": "node index.js",
    "deploy-commands": "node deploy-commands.js",
    "lint": "eslint .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "dependencies": {
    "@discordjs/builders": "^0.9.0",
    "@discordjs/rest": "^0.1.0-canary.0",
    "discord-api-types": "^0.25.2",
    "discord.js": "^13.3.1",
    "dotenv": "^10.0.0",
    "isomorphic-fetch": "^3.0.0"
  },
  "devDependencies": {
    "eslint": "^8.5.0"
  }
}
